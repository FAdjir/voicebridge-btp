[project]
name = "voicebridge-btp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-genai>=1.63.0",
    "gspread>=6.2.1",
    "pytest>=9.0.2",
    "python-dotenv>=1.2.1",
    "python-telegram-bot>=22.6",
]
[tool.pytest.ini_options]
pythonpath = [
  "."
]
